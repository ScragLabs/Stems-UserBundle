{% extends 'StemsPageBundle:Layout:' ~ page.layout.slug ~ '.html.twig' %}

{% import 'StemsCoreBundle:Macro:core.html.twig' as macros %}

{% block breadcrumbs %}<a href="{{ path('stems_front_user_account') }}">Account</a> › <a href="{{ path('stems_front_user_change_password') }}">Change Password</a>{% endblock %}

{% block dynamic %}
	<form class="full-form" method="post">
		<p class="center">Your new password must be at least 8 characters long.</p>
		{{ form_row(form.password) }}
		{{ form_row(form.passwordConfirmation) }}
		<input type="submit" value="Change Password" class="form-button no-icon"/>
		{{ form_widget(form._token) }}
	</form>
{% endblock %}